/*
  (Strategy Pattern) (Also a product in Abstract Factory Pattern) Strategy containing the implementation for GasPump2's StorePin action.
 */
public class StorePin2 extends StorePin {
  @Override
  public void StorePin(DataStore ds) {
    // Does nothing for now
  }
}
