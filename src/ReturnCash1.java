/*
  (Strategy Pattern) (Also a product in Abstract Factory Pattern) Strategy containing the implementation for GasPump1's ReturnCash action.
 */
public class ReturnCash1 extends ReturnCash {
  @Override
  public void ReturnCash(DataStore ds) {
    // Does nothing
  }
}
